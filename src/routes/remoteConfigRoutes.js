const express = require('express');
const { getRemoteConfigProperties } = require('../controllers/remoteConfigController');
const router = express.Router();

//router.get('/', getRemoteConfigProperties);

module.exports = router;